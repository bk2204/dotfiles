setxkbmap -option ctrl:nocaps -option compose:lwin
xmodmap ~/.Xmodmap
zsh -c '
if which mate-session >/dev/null 2>&1
then
	mate-session
else
	/etc/xdg/xfce4/xinitrc -- /etc/X11/xinit/xserverrc
fi
'
# vim: set ft=sh:
