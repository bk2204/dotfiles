#!/bin/sh

INPUT="$1"
BASENAME=`basename $INPUT .txt`
UPGRADE_SCRIPT="$HOME/text/xml/schemata/docbook5/tools/db4-upgrade.xsl"

asciidoc -d article -b docbook -o $BASENAME.db4 $INPUT
xml-convert \
	-t style -s $UPGRADE_SCRIPT -i $BASENAME.db4 -e \
	-t null -o $BASENAME.dbx -e
